function fruit(fruitName, weight, price){
    let weightnKilo =weight/1000; 
    let money = Number(price*weightnKilo).toFixed(2);


  console.log(`I need $${money} to buy ${weightnKilo.toFixed(2)} kilograms ${fruitName}.`);
}
fruit('orange', 2500, 1.80);
